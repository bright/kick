#!/usr/bin/env ruby

#puts Dir.pwd
require 'kick'

arg = ARGV[0]
distribution_list = ARGV[1]
#puts "Argument: #{arg}"

case arg
when "build"
  kick = Kick.new
  kick.build
when "dist"
  kick = Kick.new
  kick.dist(distribution_list)
else
  puts "Unknown command #{arg}"
end
